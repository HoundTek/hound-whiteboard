# 工具文档

本文档提供白板中工具的概述。

## 工具的定义

工具是设备与白板交互的媒介。不同的工具使设备在操作白板时会有不同的行为。一个设备只能绑定一个工具。比如你可以将选择工具、笔工具（每个笔刷都是一个工具）等绘图工具绑定到手指、鼠标等绘图设备上。

## 工具栈

工具的选择可以构成一个栈，而当前选取的工具永远是栈顶的那一个。

比如：

> 你用 DOM 操作（即点击下方面板）将当前工具切换为“选择工具”，由于“对象选择工具”是一级工具（不依托于特定对象的工具），所以它会先将当前的工具栈清空再入栈。
> 
> 然后，你选了一个多边形，“通用对象修改工具”就会入栈并成为你当前正在使用的工具。
> 
> 你又在菜单中选取“编辑多边形”功能，“多边形对象修改工具”就会入栈并成为你当前正在使用的工具。
> 
> 修改完后，你退出了编辑，栈顶弹出，当前正在使用的工具变为了“通用对象修改工具”。
> 
> 最后，你点击白板的空白处，栈顶弹出，当前正在使用的工具回退到“对象选择工具”。

## 工具的类别

### 对象创建工具

对象创建工具是用来创建对象的工具，对于每种对象，都有一个对应的对象创建工具。

比如笔刷工具就是一种对象创建工具。笔刷工具也有很多种，比如铅笔、钢笔等。

### 对象修改工具

对象修改工具是用来修改对象的工具，对于每种对象，都有一个对应的对象修改工具。

所有对象都有一些通用属性——位置和变换矩阵，而其它的细节因对象而异。又因为对于对象细节的修改不是一件频繁的事（假设你想修改一个正方形，大多数时候你只会去改它的位置，或旋转、拉伸它，而不是修改它的颜色，甚至顶点的排布）。所以，有一个通用的对象修改工具，还有针对于每一个对象类型的修改工具。要修改对象的时候，默认应用的是通用对象修改工具。

### 对象选择工具

对象选择工具是用来选择对象的工具，适用于所有对象。

对象选择工具有多种形状。比如套索工具可以框选任意曲线内的对象（在底层，曲线其实是一个有非常多条边的多边形）；矩形选择工具可以框选一个框形内的对象。特别地，所有的对象选择工具都应支持将点选识别为单选。

### 对象擦除工具

对象擦除工具是用来擦除对象的工具，适用于可擦对象。

对象擦除工具也有多种形状。它于套索工具的差别主要是它会直接修改对象，并且是将对象的一部分直接擦掉。

### 白板工具

白板工具是用于改变白板状态的工具，适用于白板而不是对象。

白板工具可以给白板加页、删页、更改页面位置等。白板工具较为特殊，因为它是直接跟白板（而不是对象）打交道的。它不可以显式地进入工具栈中，而是在 `BoardManager` 当中存在，设备欲改变白板状态则需从 `BoardManager` 处获取其引用（通常以显示器设备为媒介）。

## 工具的持久化

Hound Whiteboard 内置了很多的基础工具，但是这远不能满足用户自定义的需求。对于工具而言，用户自定义的方法有两种——插件或工具包。工具包就是用工具配置的持久化实现的，它是基于 Hound Whiteboard 内置的基础工具实现的。
