1. 基础动态行列表结构
2. 基础动态列列表结构
3. 基础动态网格结构
  - 说明：网格的单元格尺寸相同，单元格有最小宽度，整个网格有宽度设定，单元格宽度会根据内容自适应，但不会小于最小宽度。
4. 并列内容响应式布局结构
  - 说明：两个并列内容根据情况选择横向布局或纵向布局，注意水平布局时两个内容的文档流分离，垂直布局时两个内容的文档流合并。
5. 分页结构
  - 说明：分页结构用于在内容量较大的情况下，将内容分页展示，每页展示固定数量的内容。
  - 说明：分页结构需要考虑分页的导航，包括上一页、下一页、第一页、最后一页等按钮。