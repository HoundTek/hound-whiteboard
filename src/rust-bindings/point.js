/**
 * @module point
 */

const native = require("../../native");

module.exports = {
  /**
   * 二维点
   * @class
   * @description 表示二维平面上的一个点，包含 x 和 y 坐标，支持矩阵变换
   * @author Zhou Chenyu
   */
  Point: native.Point,
};
